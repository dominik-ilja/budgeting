# Entities

## Import Profile

An import profile represents a configuration for importing a CSV file into a particular database table. Different import profiles can be configured for different tables. Each table will have its own unique set of rules in how it can be interacted with.